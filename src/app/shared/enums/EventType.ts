export namespace EventType {
    export enum ActionTouch {
        TOUCHSTART = 'touchstart',
        TOUCHEND = 'touchend'
    }

    export enum ActionSwipe {
        SWIPELEFT = 'swipeleft',
        SWIPERIGHT = 'swiperight'
    }
}
